# `rig.meta.named`

Display name and durable id. Compose onto entities that need a label.

| Field | Type | Meaning |
|-------|------|---------|
| `name` | string | Display name |
| `stableId` | string | Optional durable id (empty = none) |

Do not re-declare `name` on domain schemas.
